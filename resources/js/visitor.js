import './bootstrap.js';

// Visitor-specific JavaScript
document.addEventListener('DOMContentLoaded', function() {
    // Livewire will handle most visitor interactions
    console.log('Visitor interface loaded');
});